import { BrowserRouter } from "react-router-dom";
import RoutesWrapper from "./Router";

const App = () => {
  return (
    <BrowserRouter>
      <RoutesWrapper />
    </BrowserRouter>
  );
};

export default App;
